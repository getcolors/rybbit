#!/usr/bin/env -S uv run --script
# /// script
# requires-python = ">=3.11"
# dependencies = ["package-rybbit-blue", "blue", "package-once-blue"]
#
# [tool.uv.sources]
# package-rybbit-blue = { git = "https://github.com/getcolors/rybbit.git", rev = "ad30cdcbe8757adcc5b6349f7d307ee35f98df3f", subdirectory = "blue" }
# blue = { git = "https://github.com/getcolors/blue.git", rev = "290f313ead5ca162875c33a049c880da017eae09" }
# package-once-blue = { git = "https://github.com/getcolors/once.git", subdirectory = "blue", rev = "38e3cd66674a32fb96605e1b17ae6791086ad5c1" }
#
# [tool.uv]
# override-dependencies = ["blue @ git+https://github.com/getcolors/blue.git@290f313ead5ca162875c33a049c880da017eae09"]
# ///
#
# Managed by `bb pin` — do not edit by hand, and never invent a SHA.
#
# Stamped by `bb pin`. RYBBIT_LIB_ROOT=/path/to/rybbit still overrides the
# pin with a working tree.
"""Thin launcher for package-rybbit-blue.

This file is both the skill payload and the repository entry point —
blue/blue in the repository is a symlink to it. It deliberately holds no
logic of its own: validation, the graph and the steps all live in the
package-rybbit-blue library, where the test suite reaches them. A copied
payload is the one place in this project where code cannot be tested, so
nothing that can live elsewhere should live here.
"""

import os
import subprocess
import sys

UNPINNED_ERROR = """\
this launcher carries no rybbit pin.
It has not been stamped by `bb pin`, which cannot run until the rybbit
repository has been pushed.
Point it at a checkout meanwhile: RYBBIT_LIB_ROOT=/path/to/rybbit
(or run through the repository project: cd rybbit/blue && uv run python -m package_rybbit_blue ...)
"""


def _lib_root_project(root: str) -> str:
    """The uv project inside a RYBBIT_LIB_ROOT checkout: the repository
    root or its blue/ implementation both work."""
    if os.path.isfile(os.path.join(root, "pyproject.toml")):
        return root
    return os.path.join(root, "blue")


def main() -> None:
    root = os.environ.get("RYBBIT_LIB_ROOT")
    if root:
        result = subprocess.run(
            ["uv", "run", "--project", _lib_root_project(root),
             "python", "-m", "package_rybbit_blue", *sys.argv[1:]])
        raise SystemExit(result.returncode)
    try:
        from package_rybbit_blue import exec  # resolvable only once pinned
    except ImportError:
        sys.stderr.write(UNPINNED_ERROR)
        raise SystemExit(2)
    exec()


if __name__ == "__main__":
    main()
